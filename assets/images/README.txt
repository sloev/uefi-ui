test.png — sample asset for the UEFI demo (`uefi_ui::png` decode + scaled blit).
Move replacement images here; update `include_bytes!` paths in `crates/uefi_ui_demo/src/main.rs` if the filename changes.
